// Site assets (not synced event photos) uploaded once directly to R2 —
// see the "Hero video" note in README.md for how to replace this.
export const HERO_VIDEO_URL = `${process.env.R2_PUBLIC_BASE_URL}/_site-assets/hero-video.mp4`;
